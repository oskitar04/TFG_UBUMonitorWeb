<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>
  
  <parent>
	<groupId>org.springframework.boot</groupId>
	<artifactId>spring-boot-starter-parent</artifactId>
	<version>3.2.0</version>
 	<relativePath/>
  </parent>
    
    
  <groupId>com.tfg</groupId>
  <artifactId>ubumonitor_api_rest</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>ubumonitor_api_rest</name>
  
  
  <packaging>jar</packaging> <!-- <packaging>war</packaging> -->
  
  
  <properties> <!-- Para ajustar la versión de java al JDK, en mi caso la versión 20 -->
  	<maven.compiler.source>25</maven.compiler.source>
  	<maven.compiler.target>25</maven.compiler.target>
  	<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
  </properties>
  <dependencies>
    <dependency>
      <groupId>junit</groupId> <!-- librería estandar en java para hacer pruebas unitarias (verificar que el código funciona bien) -->
      <artifactId>junit</artifactId>
      <version>3.8.1</version> <!-- <version>3.11.10</version> Esta es más nueva, la puesta es versión antigua -->
      <scope>test</scope> <!-- se refiere a que la librería solo se usará para jecutar pruebas y no se incluirá cuando exporte la API final (archivo .jar)-->
    </dependency>
    <!-- Para crear controladores REST y usar servidor Tomcat embebido. El motor principal de la API REST -->
    <dependency>
        <groupId>org.springframework.boot</groupId> <!-- -->
        <artifactId>spring-boot-starter-web</artifactId> 
        <version>3.2.0</version> <!-- Puedes ajustar la versión según tu Java -->
        <scope>test</scope>
    </dependency>

    <!-- Opcional: Para que los cambios se apliquen sin reiniciar (DevTools) -->
    <!-- API REST -->
    <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-devtools</artifactId> <!-- <artifactId>spring-boot-starter-web</artifactId> -->
        <version>3.2.0</version>
        <optional>true</optional>
    </dependency>
    
    
    <!-- Lombok (como te pide el tutor) -->
    <dependency>
        <groupId>org.projectlombok</groupId>
        <artifactId>lombok</artifactId>
        <optional>true</optional>
    </dependency>

    <!-- Swagger para probar la API -->
    <dependency>
        <groupId>org.springdoc</groupId>
        <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
        <version>2.5.0</version>
    </dependency>

    <!-- Para parsing JSON -->
    <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-databind</artifactId>
    </dependency>
    
    
  </dependencies>
  <build>
    <finalName>ubumonitor_api_rest</finalName>
    <plugins>
        <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-compiler-plugin</artifactId>
            <version>3.11.0</version>
            <configuration>
                <source>20</source>
                <target>20</target>
            </configuration>
        </plugin>
    </plugins>
  </build>
</project>
