get:
  tags:
    - Site
  summary: Get site info
  operationId: SiteInfo
  description: Get information about the Moodle site, such as site name, version, and other
  responses:
    '200':
      description: Site information
      content:
        application/json:
          schema:
            $ref: '../../components/schemas/site/site_info_response.yaml#/SiteInfoResponse'
    '401':
      $ref: '../../components/responses/unauthorized_401.yaml'
